## Summary
- Frontend: completed staking flow with empty states, toasts, instrumentation and data-testids.
- Backend: package endpoint returns resource-mapped packages; added structured logs for staking actions; kept sanctum protection.
- Seeders: demo user with verified email and sufficient balance; ≥3 active packages present by default.
- Shared types: aligned usage in UI; fixed API mappings in frontend services.
- Docs: minimal E2E path (login → see packages → invest → confirm in RealTimeInvestments).

## Changes
- api/apps/backend
  - StakingController@getPackages now returns StakingPackageResource collection and logs packages_fetched.
  - StakingController@createInvestment logs investment_failed with context on errors.
  - StakingService@getAvailablePackages now returns a Collection for proper resource mapping.
- pi-staking-frontend
  - services/stakingService: fixed response parsing for /staking/packages and /staking/investments; normalized fields; added types.
  - components/StakingPackages: now filters by is_active, shows empty state if none, uses funds source, adds confirmation modal toasts, instrumentation (packages_loaded, invest_*), and data-testids.
  - components/RealTimeInvestments: parses investments correctly, uses total_claimed, adds instrumentation and data-testids.
  - components/EmailVerifiedGuard: added resend CTA testid.
  - lib/config: env flags respected; .env.example updated with VITE_API_BASE_URL, VITE_ENABLE_EMAIL_VERIFICATION, VITE_ENABLE_WELCOME_BONUS.
  - docs/e2e-staking.md: minimal E2E path and testids.

## Why
- Ensures demo user can invest end-to-end with sane defaults and quick issue diagnosis via logs and console events.

## Impact
- Frontend relies on new response mapping; no breaking API changes besides /staking/packages now returning resource-shaped fields (kept names compatible).
- Added logs improve observability without exposing sensitive data.

## Acceptance
- Fresh seed: demo user sees ≥1 active package, can invest with funds; portfolio updates in RealTimeInvestments.
- Empty states and helper texts visible where appropriate.
- API errors surfaced via toasts; logs (backend) include user_id, package_id, amount, outcome, error_message when applicable.
- Shared types in usage align with API (no TS errors in staking flow components/services).

## Test IDs & Events
- data-testids: staking-packages, packages-grid, packages-empty, packages-retry-btn, invest-cta-{id}, continue-invest-btn, confirm-invest-btn, realtime-investments, realtime-investments-table, resend-verification-btn.
- console.info events: packages_loaded, invest_clicked, invest_success, invest_error, investments_loaded, investments_error.


₍ᐢ•(ܫ)•ᐢ₎ Generated by [Capy](https://capy.ai) ([view task](https://capy.ai/project/573006dd-84af-11f0-a94e-3eef481a796b/task/cb91566e-e1a1-486e-8fbe-ffcf5d035884))